<section id="education">
  <div class="container">
    <h2 class="section-title">Education</h2>
    
    <div class="education-layout">
      <div class="image-column orange-glow" data-spotlight>
        <img src="/images/virginia.jpg" alt="Virginia Tech" class="base-image" />
        <div class="overlay"></div>
        <div class="brightness-spot"></div>
        <img src="/images/vt_logo.png" alt="Virginia Tech Logo" class="school-logo logo-bottom-right" />
      </div>
      
      <div class="text-column">
        <h3>Virginia Tech</h3>
        <p class="degree">Bachelor of Science in Computer Science</p>
        <p class="year">2022 - 2024</p>
        <p class="description">
          Developed a strong foundation in algorithms, problem-solving, and software development. Designed algorithms and data structures. Transferred in 2024. 
        </p>
      </div>
    </div>

    <div class="education-layout reverse">
      <div class="image-column" data-spotlight>
        <img src="/images/bu5.jpg" alt="Boston University" class="base-image" />
        <div class="overlay"></div>
        <div class="brightness-spot"></div>
        <img src="/images/bu_logo.png" alt="Boston University Logo" class="school-logo logo-bottom-left" />
      </div>
      
      <div class="text-column">
        <h3>Boston University</h3>
        <p class="degree">Bachelor of Arts in Computer Science</p>
        <p class="year">2024 - Present</p>
        <p class="description">
          Focused on software engineering, algorithms, data structures, and artificial intelligence. 
          Expected to graduate in May 2026.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  section {
    min-height: 100vh;
    padding: 4rem 2rem 25vh 2rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: #000000;
    border-top: 1px solid #1a1a1a;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    width: 100%;
  }

  .section-title {
    font-size: 3.5rem;
    font-weight: 800;
    margin-bottom: 4rem;
    text-align: center;
    color: white;
    position: relative;
    padding-bottom: 1rem;
  }

  .section-title::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 100px;
    height: 4px;
    background: linear-gradient(90deg, transparent, rgb(153, 82, 228), transparent);
  }

  .education-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
    margin-bottom: 5rem;
  }

  .education-layout:last-child {
    margin-bottom: 0;
  }

  .education-layout.reverse {
    grid-template-columns: 1fr 1fr;
  }

  .education-layout.reverse .image-column {
    order: 2;
  }

  .education-layout.reverse .text-column {
    order: 1;
    border-left: none;
    border-right: 2px solid #444;
  }

  .image-column {
    position: relative;
    width: 100%;
    aspect-ratio: 4 / 3;
    border-radius: 12px;
    overflow: hidden;
    cursor: none;
    border: 2px solid #444;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5),
                0 0 0 1px rgba(255, 255, 255, 0.4);
    transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275), 
                border-color 0.3s ease, 
                box-shadow 0.3s ease;
  }

  .image-column:hover {
    transform: scale(1.05);
    border-color: #ff0040;
    box-shadow: 0 25px 60px rgba(0, 0, 0, 0.6),
                0 0 20px rgba(255, 0, 64, 0.6),
                0 0 40px rgba(255, 0, 64, 0.4),
                0 0 60px rgba(255, 0, 64, 0.2),
                inset 0 0 20px rgba(255, 0, 64, 0.1);
  }

  .image-column.orange-glow:hover {
    border-color: #ff8c42;
    box-shadow: 0 25px 60px rgba(0, 0, 0, 0.6),
                0 0 20px rgba(255, 140, 66, 0.6),
                0 0 40px rgba(255, 140, 66, 0.4),
                0 0 60px rgba(255, 140, 66, 0.2),
                inset 0 0 20px rgba(255, 140, 66, 0.1);
  }

  .base-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.75);
    pointer-events: none;
    transition: opacity 0.3s ease;
  }

  .brightness-spot {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.2s ease;
  }

  .image-column:hover .brightness-spot {
    opacity: 1;
  }

  .school-logo {
    position: absolute;
    width: 100px;
    height: auto;
    opacity: 0;
    transition: opacity 0.4s ease, transform 0.4s ease;
    z-index: 3;
    filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.5));
    transform: scale(0.9);
  }

  .image-column:hover .school-logo {
    opacity: 1;
    transform: scale(1);
  }

  .logo-bottom-right {
    bottom: 1.5rem;
    right: 1.5rem;
  }

  .logo-bottom-left {
    bottom: 1.5rem;
    left: 1.5rem;
  }

  .text-column {
    color: white;
    padding: 2rem;
    border-left: 2px solid #444;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .text-column h3 {
    font-size: 2rem;
    font-weight: 700;
    margin: 0 0 1rem 0;
    color: white;
  }

  .degree {
    font-size: 1.25rem;
    color: #aaa;
    margin: 0 0 0.5rem 0;
  }

  .year {
    font-size: 1rem;
    color: #888;
    margin: 0 0 1.5rem 0;
  }

  .description {
    font-size: 1rem;
    line-height: 1.8;
    color: #ccc;
    margin: 0;
  }

  @media (max-width: 900px) {
    .education-layout {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .image-column {
      max-width: 600px;
      margin: 0 auto;
    }
  }

  @media (max-width: 768px) {
    section {
      padding: 2rem 1rem 15vh 1rem;
    }

    .section-title {
      font-size: 2rem;
      margin-bottom: 2rem;
    }

    .text-column h3 {
      font-size: 1.75rem;
    }

    .text-column p {
      font-size: 1rem;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const spotlights = document.querySelectorAll('[data-spotlight]');
    
    spotlights.forEach((spotlight) => {
      const overlay = spotlight.querySelector('.overlay');
      const brightnessSpot = spotlight.querySelector('.brightness-spot');
      if (!overlay || !brightnessSpot) return;

      spotlight.addEventListener('mousemove', (e) => {
        const rect = spotlight.getBoundingClientRect();
        const x = e.clientX - rect.left;
        const y = e.clientY - rect.top;

        // Sharper mask with less feathering
        overlay.style.maskImage = `radial-gradient(circle 100px at ${x}px ${y}px, transparent 0%, transparent 85%, black 100%)`;
        overlay.style.webkitMaskImage = `radial-gradient(circle 100px at ${x}px ${y}px, transparent 0%, transparent 85%, black 100%)`;
        
        // Brightness boost in revealed area
        brightnessSpot.style.background = `radial-gradient(circle 100px at ${x}px ${y}px, rgba(255, 255, 255, 0.3) 0%, rgba(255, 255, 255, 0.15) 50%, transparent 100%)`;
      });

      spotlight.addEventListener('mouseenter', () => {
        overlay.style.maskImage = '';
        overlay.style.webkitMaskImage = '';
      });

      spotlight.addEventListener('mouseleave', () => {
        overlay.style.maskImage = '';
        overlay.style.webkitMaskImage = '';
        brightnessSpot.style.background = '';
      });
    });
  });
</script>

